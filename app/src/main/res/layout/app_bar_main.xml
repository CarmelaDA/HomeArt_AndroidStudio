<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/Theme.HomeArt.AppBarOverlay"
        app:layout_anchor="@+id/appBarLayout"
        app:layout_anchorGravity="center">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#8C1252"
            app:popupTheme="@style/Theme.HomeArt.PopupOverlay" />

    </com.google.android.material.appbar.AppBarLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/butt_ajustes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:contentDescription="@string/button"
        android:focusable="true"
        android:translationX="345dp"
        android:translationY="8dp"
        app:fabSize="mini"
        app:srcCompat="@drawable/homeart_marcaagua"
        tools:ignore="TouchTargetSizeCheck,RedundantDescriptionCheck" />

    <include
        layout="@layout/content_main"
        android:layoutDirection="locale"
        android:layoutMode="opticalBounds" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>